version: 2.1

workflows:
  test-env-vars:
    jobs:
      - build:
          context: SAMPLE # has an env var called MY_ENV_VAR

jobs:
  build:
    docker:
      - image: cimg/base:2020.01
    steps:
      - checkout
      - run: 
          name: "echo an env var that is part of our context"
          command:
            echo "${KANKYO_HENSU_MEI}"